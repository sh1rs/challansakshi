# Optional. The complete demo works without these values.
OPENAI_API_KEY=
OPENAI_MODEL=

# Set this to the trusted deployed origin so Open Graph URLs are absolute.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
